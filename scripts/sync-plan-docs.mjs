import assert from "node:assert/strict";
import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
import { fileURLToPath } from "node:url";
import {
  audit,
  auditChecks,
  budget,
  budgetNotes,
  citations,
  fixes,
  formatEok,
  hardwareRisks,
  hero,
  journey,
  kit,
  kpis,
  locks,
  pipes,
  platformStrategy,
  schedule,
  supply,
  toWon,
  years,
} from "../src/data/plan-v2.ts";
import {
  cloudNotice,
  demoNotice,
  engineNotice,
  positioning,
  privacyNotice,
} from "../src/data/product-policy.ts";

const root = new URL("../", import.meta.url);
const table = (head, rows) =>
  [head, head.map(() => "---"), ...rows]
    .map(
      (row) =>
        `| ${row.map((value) => String(value).replaceAll("|", "\\|").replaceAll("\n", " ")).join(" | ")} |`,
    )
    .join("\n");
const list = (lines) => lines.map((line) => `- ${line}`).join("\n");
const checksTable = table(["검산 항목", "계산·대조", "결과"], audit);
const fundsTable = table(["재원 항목", "금액(억)", "사용처"], pipes);
const yearsTable = table(["연차", "국비", "지방비", "민간·수요", "계", "주요 내용"], years);

assert.equal(auditChecks.length, 6);
for (const check of auditChecks) {
  assert.ok(
    check.ceiling ? check.actual <= check.expected : check.actual === check.expected,
    check.label,
  );
}
assert.equal(budget.national + budget.local + budget.privateShare, budget.total, "재원 합계");
assert.deepEqual(
  [budget.total, budget.national, budget.local, budget.privateShare],
  [5_000_000_000, 3_240_000_000, 800_000_000, 960_000_000],
  "고정 예산 전제",
);
assert.deepEqual([budget.units, budget.unitPrice, budget.supportPercent], [1500, 1138000, 80]);
assert.equal(hero.period, "2028–2030");
assert.equal(supply.support, 1_365_600_000);
assert.equal(supply.allocation - supply.support, 4_400_000);
assert.equal(supply.own, 341_400_000);
assert.equal(
  kit.slice(0, 4).reduce((sum, row) => sum + Number(row[1].replaceAll(",", "")), 0),
  budget.unitPrice,
);
for (const row of years) {
  assert.equal(toWon(row[1]) + toWon(row[2]) + toWon(row[3]), toWon(row[4]), row[0]);
}
assert.equal(citations.length, 11);

const plan = `# 보이스아이 사업계획서 v2.1

심의 준비안 · 투명 회의 패널 전환 2026-09-04

> 예산 원본: src/data/plan-v2.ts. 공통 운영 문구: src/data/product-policy.ts. 이 문서는 npm run docs:sync로 생성한다. 부록 11건은 미검증이며 재원 확보·인증 기기 선정·현장 성능 검증이 완료된 제출본이 아니다.

## 1. 사업 개요와 필요성

${hero.subtitle}. ${hero.line}.

청각장애인이 공공·직장 회의에서 발언 내용을 읽고 발언자를 확인하며, 노트북으로 자신의 의사를 전달할 수 있도록 지원한다. 노트북 위 투명 패널을 통해 참석자의 얼굴과 몸짓을 보면서 확정 자막·화자 위치를 함께 확인한다. 비공개 발언 초안은 노트북에만 두고 확인한 문장만 패널과 스피커로 보낸다. 장애인 전용 외형을 강요하지 않으며 당사자 선호와 통역 선택권을 유지한다.

${positioning}

총사업비 **${hero.total}** = 국비 **${hero.national}억** + 지방 **${hero.local}억** + 민간·수요 **${hero.privateShare}억**. 사업기간 **${hero.period}**, ${hero.pre}. 누적 보급 목표 **${hero.kits}**.

연계 협의 대상은 ${hero.ministries}이며 부처의 지원 확약을 뜻하지 않는다. 실수요 ${hero.demand}명은 미검증 추정으로 모집·보급 타당성의 확정 근거가 아니다. 등록 인구·연령·경제활동 비율의 모집단 일치 여부와 회의 빈도를 사전기획 조사에서 확인한다.

## 2. 사용자 여정 및 현재 구현 범위

${journey.map(([title, body], index) => `${index + 1}. ${title}: ${body}`).join("\n")}

현재 데모는 미리 작성한 회의 대본을 재생한다. 이름은 예시 명단이며 얼굴·음성 신원 매칭 결과가 아니다. 좌석 선택은 수동 자막 우선 기능이고, 얼굴 링은 단일 사용자 시점에서 보정된 위치 표시의 개념 예시다. 여러 관찰자에게 동시에 같은 얼굴 위치로 정렬된다고 주장하지 않는다. 실제 분리·실패 감지와 양면 광학 보정은 아직 미구현이다.

${demoNotice}

${privacyNotice}

${engineNotice}

${cloudNotice}

로그인과 DB는 사용하지 않는다. LED는 실제 장치 도입 시 준수할 고지 요건이며 웹 데모가 하드웨어 LED를 제어한다는 뜻이 아니다. 화면의 참석자 확인면은 개념 시연이며 반대편 글자의 좌우반전·겹침·시야각은 실물 후보별로 검증한다.

## 3. 플랫폼·운영 전략

${platformStrategy.primary}

${platformStrategy.architecture}

${platformStrategy.hardware}

${platformStrategy.portable}

${table(["위험", "왜 문제인가", "검증 게이트"], hardwareRisks)}

사업용 처리 목표는 국내 리전 또는 온프레미스이며 국외이전을 배제한다. 현재 xAI TTS 데모가 이 요건을 충족한다고 주장하지 않는다. 합성음 테스트와 현장 회의 실증을 구분하고 실증 전 데이터 흐름·보존·처리 지역·동의 요건을 확인한다.

## 4. 성과지표 및 평가

아래는 목표이며 실측 성과가 아니다. WER·DER 평가는 4~8인 원탁, 화자–마이크 평균 1.5m, 배경소음 45dBA, 중첩발화 10% 이상 조건을 목표 프로토콜로 두고 실제 전사·화자 주석과 대조한다. 지연은 발화 종료부터 화면 표시까지 정의해 중앙값과 상위 지연, 네트워크·장치 조건을 함께 기록한다. 패널 자막 판독률은 거리·시야각·조도·배경 대비를 고정해 측정한다.

${table(["지표", "사전기획 2027", "2028", "2029", "2030"], kpis)}

ASR 실측 원본은 src/data/asr-results.json이며 /bench에서 요약·문장별 결과와 JSON 다운로드를 제공한다. 동일 벤더 TTS→STT 왕복은 낙관 편향이 있다. 원거리는 잔향·핑크노이즈 합성, 겹침은 0.9초 믹스다. 실제 테이블 마이크·다화자 회의·방언 성능으로 일반화하지 않는다. JSON의 과거 글래스 개념 문장은 벤치 재현을 위해 보존하며 현재 플랫폼 전략의 근거가 아니다. 현장 DER와 스트리밍 종단 지연은 미측정이다.

## 5. 보급 키트

${table(["구성", "단가(원)", "계정", "내구연한"], kit)}

패널 본체 850,000원은 복수 견적 전 편성 상한 가정이다. 노트북은 사용자·기관 보유 자산으로 보고 키트 단가에서 제외한다. 기기 비용과 운영·서비스비를 분리한다. 80%·90% 지원율은 제도 확인 전 가정이며, 본 사업 검산은 80%만 사용한다. 운영 라이선스 연 180,000원을 4년 반영한 계획비는 설치·적합·훈련 120,000원 포함 1,978,000원이며 교체·수리비는 빠져 있다.

## 6. 재원 및 연차별 편성

${fundsTable}

${yearsTable}

## 7. 검산표 6항

${checksTable}

${list(budgetNotes)}

검산 통과는 산술 및 배정 한도 확인이며 정책 근거·재원 확약·조달 단가 검증 완료를 의미하지 않는다. 국비 보급 9.6억은 국비 총액의 일부, 지방 보급 4.1억은 지방 총액의 일부, 미지원분 ${formatEok(supply.own)}억은 민간 총액의 일부다. 부담 주체는 사용자·고용주·수요기관 협의로 정하며 같은 금액을 추가로 총사업비에 더하지 않는다.

## 8. 착수 일정과 지속재정

아래는 협의를 위한 일정안이며 공고·심의 통과를 보장하지 않는다.

${table(["시기", "산출물"], schedule)}

2030년 누적 보급 완료 후 현 단가 기준 라이선스는 연 2.7억 원이다. 온디바이스 비중을 높여 연간 세트당 운영비를 9만 원 이하로 낮추는 것을 후속 R&D 목표로 검토한다. 현재 예산의 기기가액에 운영비를 섞지 않으며, 운영비 선납 여부·기관 경상예산 이관·사용자 부담을 별도 확정한다. 종료 후 비용 부담 주체와 장애 대응 책임이 정해지기 전에는 지속 운영이 확보됐다고 보지 않는다.

## 9. 유지할 결정

${list(locks)}

## 10. 미검증 인용 11건

아래 값은 전달된 v2 원본의 기억·가정·추정이며 확정 사실로 인용하지 않는다. 제출 전 발행기관·문서명·발행일·원문 URL·페이지·검증자를 기록해 1차 출처로 대체한다.

${table(
  ["항목", "원본의 미검증 표기", "확인 대상", "상태"],
  citations.map((row) => [...row, "미검증"]),
)}

소보로·쉐어타이핑과 투명 자막 장치 등 기존 서비스의 기능 부재나 보급 범위를 단정하지 않는다. 투명 패널 자체를 신규 발명이라고 주장하지 않는다. 법령·지원율·국비 지방비 매칭·지원한도·KC·표시 성능은 연도와 적용 범위를 확인한 후 본문에 반영한다.

## 11. 제출 전 남은 산출물

- 라이브 테이블 마이크 스트리밍·익명 화자분리·같은 방향 복수 화자 분리·실패 시 방향 격하 구현 및 현장 DER·지연 실측.
- 직접 투명 패널·광학 콤바이너 후보의 KC·단가·유통·밝기·양면 가독성 비교표.
- 미검증 인용 11건의 1차 출처 확인과 예산 재원별 집행 분담·지속 운영 주체 확정.
- 당사자 참여 사용성 평가와 통역 지원 병행 계획, 장치 LED 고지·비저장 이행 검증.
`;

const changes = `# v1 → v2 → v2.1 수정내역

생성 원본: src/data/plan-v2.ts. P0 점검일 2026-09-03, 플랫폼 전환일 2026-09-04. 과거 v1 비교 내용은 fixes에 기록된 설명이며, v1 원문 전체를 별도로 검증한 것은 아니다.

${fixes.map((f) => `## ${f.title}\n\n- 등급: ${f.grade}\n- v1 기록: ${f.v1}\n- v2: ${f.v2}\n\n${f.body}`).join("\n\n")}

## v2 → v2.1 플랫폼 전환

사용자 결정에 따라 회의실 주 플랫폼을 스마트글래스에서 노트북 연동 데스크형 투명 패널로 전환했다. 글래스는 이동형 후속 옵션으로 내렸다. 패널은 확정 자막·발언자 위치를 공유하고, 비공개 발언 초안·수정·운영 설정은 노트북에만 둔다. 직접 투명 패널과 광학 콤바이너 중 밝기·양면 가독성·좌우반전·시야각·안전·KC·복수 견적을 통과한 방식을 채택한다. 이 전환은 기존 50.0억 재원표와 1,138,000원 키트 산술을 변경하지 않는다.

## P0에서 확인해 추가 교정한 사항

1. 총사업비 ${hero.total}, 국비 ${hero.national}억, 지방 ${hero.local}억, 민간 ${hero.privateShare}억, ${hero.kits}, ${hero.period}는 유지했다.
2. 잘못된 등식 13.66 = 9.6 + 4.1을 제거했다. 소요 13.656억과 배정 13.7억을 구분하고 차액 440만 원을 밝혔다.
3. 미지원액 3.414억은 민간 총액의 일부로 명시했다. 3.41 = 3.4라는 등식을 제거하고 반올림 표기를 구분했다.
4. 계획서와 /plan은 동일 데이터의 검산표 여섯 항목을 사용한다. 문서 생성 후 --check로 최신 상태를 검증한다.
5. 실제 배경 이미지의 얼굴 위치가 전달문 좌표와 달라 meeting.ts의 head만 재보정했다. 링 크기와 44px 터치 영역을 분리하고 모바일 미니맵을 축소했다.
6. 겹침 링의 실선 그림자를 제거해 화자 3·5가 동시에 점선으로 표시되도록 했다. v2.1에서는 공개 패널과 비공개 노트북 미리보기를 분리했다.
7. TTS 요청·재생 실패를 완료 자막으로 기록하던 오류를 수정했다. 편집하면 다시 미리보기가 필요하고, 중복 전송·네트워크 예외·화면 이탈 중 오디오를 처리한다.
8. 대본 리허설·실제 마이크 미사용·공통 비저장 고지·xAI 전송 범위를 HUD와 문서에 표시했다. 익명 기본값·수어통역 보완 원칙은 유지했다.
9. /bench 원본 JSON은 수정하지 않았다. 과거 벤치 게이트와 v2 목표를 구분하고 현장 DER·스트리밍 지연 미측정을 명시했다.
10. Windows에서 npm 실행이 가능하도록 Vite JS 진입점을 사용하고, 추적되는 app-env.json으로 Auth OFF 기본값을 보존했다. DB는 도입하지 않았다.

## 재검산 6항

${checksTable}

${list(budgetNotes)}

## 그대로 남은 제한

부록 인용 11건은 미검증이다. 라이브 ASR·익명 화자분리 실측·같은 방향 복수 화자 분리·패널 광학·KC 후보 검증은 후속 작업이며 이번 UI·문서 전환과 구분한다. 키 없는 TTS에서는 실패 경로만 검증할 수 있고 실제 음질·스피커 출력·현장 인식률을 확인했다고 주장하지 않는다.
`;

const strategy = `# 보이스아이 제품전략 v2.1 — 투명 회의 패널

조사·결정 기준일 2026-09-04. 공개된 제조사·서비스 공식 자료를 바탕으로 한 개발 판단이며 국내 조달 가능성·KC 적용 단위·견적·현장 성능은 별도 검증한다. 예산 부록의 미검증 인용 11건은 이 문서와 별개로 계속 미검증 상태다.

## 결정

- 회의실 주 플랫폼은 노트북 위에 설치하는 데스크형 투명 자막 패널이다.
- 노트북은 비공개 발언 초안·수정·운영 화면, 패널은 확인된 공개 자막·화자 위치 화면이다.
- 스마트글래스는 이동·현장 업무용 후속 옵션이다.
- 직접 투명 디스플레이와 광학 콤바이너를 모두 비교한 뒤 보급형을 고른다.
- 투명 패널 자체를 보이스아이의 신규 발명으로 주장하지 않는다.

## 시장 기준

1. [JDI Rælclear](https://www.j-display.com/en/product_tech/raelclear.html)는 PC 등의 음성을 텍스트로 바꿔 투명 보조 모니터에 표시하는 상용 제품이다. 공식 사양은 12.3인치 84% 투과·150cd/m², 20.8인치 80% 투과·300cd/m², HDMI 입력과 수평 반전을 제시한다. 직접 투명 패널 후보의 기준선으로 삼는다.
2. [TOPPAN UCDisplay Air](https://www.holdings.toppan.com/ja/news/2026/newsrelease260325_1.html)는 2026년 3월 출시된 눈높이 공간 설치형 투명 번역 디스플레이다. 공식 참고가는 세전 990,000엔이며 설치 조건에 따라 변동한다. 패널 본체 850,000원 가정이 검증된 견적이 아님을 보여 주는 비교 근거다.
3. [VUEVO](https://vuevo.net/service/)는 8마이크·360° 음원 방향·최대 3방향 동시 인식과 텍스트 표시를 제공한다. [공식 설명서](https://www.education.ebrand.jp/vuevo/manual/)는 같은 방향에서 여러 사람이 말하면 동일 발언자명으로 표시된다고 밝힌다.
4. [See-Through Captions](https://arxiv.org/abs/2101.11326)는 투명 화면에서 자막과 상대의 표정·몸짓을 함께 보는 접근을 다룬 선행연구다. 일대일 시제품 근거를 다중 회의 성과로 확대 해석하지 않는다.

## 차별화 가설

**카메라의 세션 한정 얼굴 추적과 마이크 어레이의 음원 방향을 결합해 같은 방향·동시 발화에서도 사람 단위로 한국어 자막을 연결하고, 참석자가 공유 화면을 보며 오인식을 즉시 표시한다.**

얼굴 추적은 신원 인식이 아니라 회의 중 익명 트랙 유지에 한정한다. 기본값은 화자 1·2 같은 세션 클러스터이며 이름은 주최자가 수동으로 붙인다. 추적 신뢰도가 낮으면 이름을 확정하지 않고 좌석 방향과 확인 필요 상태로 격하한다.

## 기록 완전성 점검

기본 운영은 비저장이다. 주최자가 참석자에게 고지하고 기록 모드를 켜면 텍스트만 저장하는 방식을 기본으로 하며, 음성·영상 저장은 별도로 선택한다. 기록 모드는 음성 감지 후 자막이 확정되지 않은 구간, 겹침 발화, 낮은 전사 신뢰도, 마이크 단절, 화자 미확정을 누락 후보로 남긴다. 회의 후에는 전체 녹취를 다시 듣는 대신 후보 구간만 재생해 수정·확정하고, 결정·담당자·기한을 원문 구간과 연결해 TXT·PDF·SRT·VTT로 내보낸다.

화면에는 완전성을 보장하는 “누락 없음” 대신 “탐지된 누락 후보 없음”을 쓴다. 누락 후보의 이벤트 스키마는 시각, 유형, 지속시간, 익명 화자 후보, 신뢰도, 입력 장치 상태로 구성하고, 평가 지표는 누락 구간 탐지 재현율과 잘못 표시한 후보 비율을 함께 본다.

## 개발 순서

1. **소프트웨어 흐름 검증:** 현재 웹 데모에서 공개 패널·비공개 노트북·좌석 우선·겹침·오인식 표시·확인 후 TTS를 사용자 평가한다.
2. **저비용 광학 시제품:** 국내 적합등록 완료 모니터와 모서리를 보호한 반투명 콤바이너로 15인치급 시제품을 만들고, 글자 크기·2줄 배치·배경 대비·눈 피로를 확인한다. 단면 시점용임을 명시한다.
3. **직접 투명 패널 비교:** JDI 12.3·20.8인치급 샘플 대여 또는 견적을 받아 콤바이너와 밝기·투과·양면 판독·반사·시야각을 같은 조건에서 비교한다.
4. **다중 화자 융합:** 카메라 익명 트랙·8채널급 음원 방향·화자분리를 결합하고 같은 방향 복수 화자와 중첩발화 데이터셋으로 평가한다.
5. **보급 게이트:** 최종 조립 상태의 KC/안전 분류, 전도·모서리·전원, A/S·불량 교환·부품 공급, 공식 견적 3종을 확인한 뒤 20세트 알파로 간다.

## 비교 실험 지표

| 영역 | 지표 | 비교 기준 |
|---|---|---|
| 전사 | 한국어 CER/WER, 숫자·고유명사 정확도 | 일반 노트북 자막 |
| 화자 | DER, 동일 방향 복수 화자 귀속률, 중첩발화 분리율 | 방향 단독 표시 |
| 지연 | 확정 자막 중앙값·p95 지연 | 0.8초 목표와 현장 기준선 |
| 화면 | 조도·거리·시야각별 판독 성공률, 오류 확인 시간 | 불투명 노트북 화면 |
| 참여 | 시선 이탈 횟수, 이해도, 발언 참여, 자기효능감 | 기존 지원 방식 병행 |
| 기록 | 누락 구간 탐지 재현율, 오탐률, 검토 시간 | 전체 녹취 수동 재검토 |
| 안전 | 전도·모서리·발열·전원·케이블 사고 | 설치 전 위험성 평가 |

## 예산 연결

기기가액 소계 1,138,000원과 1,500세트·지원율 80% 가정은 기존 50.0억 검산을 유지하기 위한 편성값이다. 패널 본체 850,000원은 실제 시장가가 아니라 상한 가정이다. 노트북·교체·수리비가 빠져 있으므로 1,978,000원은 총소유비용이 아니라 4년 계획비로 표기한다. 공식 견적에 따라 수량·구성·재원 배분을 다시 검산한다.
`;

const outputs = [
  ["보이스아이_사업계획서_v2.md", plan],
  ["v1-v2_수정내역.md", changes],
  ["제품전략_v2.1.md", strategy],
];
mkdirSync(new URL("artifacts/", root), { recursive: true });
for (const [name, content] of outputs) {
  const path = new URL(`artifacts/${name}`, root);
  if (process.argv.includes("--check")) {
    assert.equal(
      readFileSync(path, "utf8").replaceAll("\r\n", "\n"),
      content,
      `${name}: npm run docs:sync 필요`,
    );
  } else {
    writeFileSync(path, content, "utf8");
  }
  console.log(`${process.argv.includes("--check") ? "검증" : "생성"}: ${fileURLToPath(path)}`);
}
console.log("예산 6항 및 재원·연차·단가 교차검산 통과; 인용 11건은 미검증.");
